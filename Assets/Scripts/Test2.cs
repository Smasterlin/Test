using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("第三次在家克隆下项目之后，然后推送仓库");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
